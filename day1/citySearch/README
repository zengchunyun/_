这个程序是基于python3.x而写的，需要用python3以上版本执行

执行方式
python3 searchInfo.py


 如果想直接运行，需要修改文件开头的运行环境
 如：#!/usr/bin/env python3
 然后加上可执行权限
  chmod a+x searchInfo.py
  
  执行
  ./searchInfo.py
  
  
  运行程序后
  首先显示的是一级菜单
  如果需要查询子菜单详细信息，请输入对应编号进行查找，退出请按q或quit
  
  选择正确序号后进入到二级菜单
  如果需要查询子菜单详细信息，请输入对应编号进行查找，退出请按q或quit
  如果要返回上一级菜单，请按b或者back
  
  选择正确序号后，将显示三级菜单
  退出请按q或quit
  如果要返回上一级菜单，请按b或者back